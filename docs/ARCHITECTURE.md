# Architecture

How mapdrill is put together: a pack-agnostic engine driving two interchangeable
modes over a shared SVG map view, with no server and no runtime dependencies.

## Engine vs modes vs UI

## Session state machine

## Rendering: inline SVG binding

## Pack loading and validation

## Build and deploy (static only)
